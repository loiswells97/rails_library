<%= link_to('< Back to targets 🎯', targets_path, :class => 'back_button')%>

<h1>Add a new reading target 🏹</h1>

<%= form_for(@target, :url => targets_path, :method => 'post') do |f| %>
<%= render(:partial => 'form', :locals => {:f => f})%>
  <div>
    <%= f.submit("Create target")%>
  </div>
<% end %>
