  <span>Read</span>
  <%= f.number_field(:target)%>
  <%= f.select(:target_type, ["books", "pages"], :prompt => 'Select type')%>
  <span>starting on</span>
  <%= f.date_field(:start_date)%>
  <span>ending on</span>
  <%= f.date_field(:end_date)%>
